apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cabotcup-cert
  namespace: envoy-gateway-system
spec:
  secretName: cabotcup-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - cabotcup.ca
