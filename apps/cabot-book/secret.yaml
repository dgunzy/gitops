apiVersion: v1
kind: Secret
metadata:
  name: cabot-book-env
  namespace: k0s-cluster-1
type: Opaque
stringData:
  # Note: This secret should be replaced with ExternalSecret when External Secrets Operator is set up
  # See MIGRATION.md Phase 8 for External Secrets setup
  APP_ENV: "production"
  # Add other environment variables here or migrate to AWS Secrets Manager

