apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: masters-pool
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: masters-pool
  filterTags:
    pattern: "^latest$"
  digestReflectionPolicy: Always
  interval: 5m
  policy:
    alphabetical:
      order: asc
