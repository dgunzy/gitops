apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: polytrader
  namespace: flux-system
spec:
  interval: 5m
  url: ssh://git@github.com/dgunzy/poly-trader.git
  ref:
    branch: main
  secretRef:
    name: polytrader-deploy-key
